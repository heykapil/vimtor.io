{
  "name": "vimtor.io",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "preact watch",
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "lint": "eslint src",
    "sass": "sass src --update sass:scss"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "build/"
    ]
  },
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-preact": "^1.1.0",
    "node-sass": "^5.0.0",
    "preact-cli": "^3.0.0",
    "sass-loader": "^10.1.0",
    "sass": "1.30.0",
    "sirv-cli": "^1.0.3"
  },
  "dependencies": {
    "preact": "^10.3.2",
    "preact-render-to-string": "^5.1.4",
    "preact-router": "^3.2.1"
  }
}
